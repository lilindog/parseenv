"use strict";

const assert = require("assert");

describe("getEnv 模块单元测试", () => {
   describe("getEnv 方法", done => {

   });
   describe("getEnvSync 方法", () => {
      describe("直接从远程url获取", () => {

      });
      describe("从包含远程include的env中获取", () => {

      });
   });
});